<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Message</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="edit-container">
        <h3>Edit Message Content</h3>
        <p class="edit-info">
            Modifying conversation between <br>
            <strong><%= chat.from %></strong> and <strong><%= chat.to %></strong>
        </p>

        <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
            <label for="msg">Message Text:</label>
            <textarea id="msg" name="msg" rows="8" placeholder="Type updated message..."><%= chat.msg %></textarea>
            
            <div class="edit-actions">
                <button class="update-btn">Update Message</button>
                <a href="/chats" class="cancel-link">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>